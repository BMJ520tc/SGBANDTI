# SGBANDTI 代码运行 / 复现说明

> 本交付包 = SGBANDTI 主代码（`code/`）+ 数据 CSV（`data/`）+ 实验结果（`00_实验结果汇总.md`）+ 调参记录（`01_调参过程与结果.md`）。
> 代码默认配置 = 定版模型（BioSNAP AUROC 0.9062±0.0019），逐位可复现。

---

## 一、环境

conda 环境 `sgbandti`（Python 3.10.20），版本矩阵：

| 包 | 版本 |
|---|---|
| torch | 2.1.0+cu121 |
| dgl | 2.2.1+cu121 |
| torch-scatter | 2.1.2+pt21cu121 |
| torchdata | **0.7.1**（新版本移除 dgl 需要的 datapipes） |
| dgllife | 0.3.2 |
| torch-geometric | 2.6.1 |
| rdkit | 2026.3.5 |
| pydantic | 2.13.4 |
| numpy / pandas / scikit-learn / matplotlib / prettytable / tqdm / yacs | 标准 |

**踩坑**：默认 pip 源 torch 是 CPU 版，必须 `pip install torch==2.1.0+cu121 -f https://download.pytorch.org/whl/torch_stable.html`；torchdata 必须 0.7.1。

## 二、数据

- `data/`：BioSNAP random / unseen_drug / unseen_target + BindingDB random 的 train/val/test CSV（**与仓库逐字节一致，MD5 已验证**）。
- **路径约定**：代码位于 `code/`，从 `code/` 运行，数据读取相对路径为 `../data/`（即代码的 `./datasets/` 已统一改为 `../data/`）；子图缓存输出到 `../data/subgraph_cache/`。结果输出到 `./result/`（即 `code/result/`）。
- 划分说明：BioSNAP 去重 7 对后 27,457；7:1:2；冷启动按 canonical SMILES / 标准化蛋白分组，三 split 实体交集=0（无泄漏）。BindingDB 无重复对。
- **子图缓存（28GB，不在交付包内）**：首次运行前必须重建：

```bash
python build_subgraph_cache.py --data biosnap --split random --hop 2
python build_subgraph_cache.py --data biosnap --split unseen_drug --hop 2
python build_subgraph_cache.py --data biosnap --split unseen_target --hop 2
python build_subgraph_cache.py --data bindingdb --split random --hop 2
```

## 三、运行命令（env `sgbandti`）

### 3.1 主实验
```bash
# BioSNAP random 5 种子（主实验）
python main.py --data biosnap --split random --hop 2 --seeds 42,52,62,72,82
# 结果: result/biosnap_random_hop2/seed_summary_stats.csv (0.9062±0.0019)

# BindingDB random 5 种子（✅ 已完成，5 种子，结果见 00_实验结果汇总 1.3 节）
python main.py --data bindingdb --split random --hop 2 --seeds 42,52,62,72,82

# 冷启动 5 种子（✅ 已完成，5 种子，结果见 00_实验结果汇总 1.2 节）
python main.py --data biosnap --split unseen_drug --hop 2 --seeds 42,52,62,72,82
python main.py --data biosnap --split unseen_target --hop 2 --seeds 42,52,62,72,82
```

### 3.2 消融 2×2（✅ 已完成 2×2×5 种子，结果见 00_实验结果汇总 二节）
```bash
python main.py --data biosnap --split random --hop 2 --seeds 42,52,62,72,82 --ablation full
python main.py --data biosnap --split random --hop 2 --seeds 42,52,62,72,82 --ablation no_subgraph
python main.py --data biosnap --split random --hop 2 --seeds 42,52,62,72,82 --ablation no_ban
python main.py --data biosnap --split random --hop 2 --seeds 42,52,62,72,82 --ablation no_both
```

### 3.3 超参微调（见 `01_调参过程与结果.md`，单种子 42 筛）
```bash
python main.py --data biosnap --split random --hop 2 --seeds 42 --tag <名称> [--dropout 0.2 --weight-decay 1e-4 | --heads 4 | --lr 1e-4 | --hidden 256 | --batch-size 32 | --max-epoch 250 | --cosine]
```

### 3.4 统计（CPU，SGBANDTI/RF 已跑；其余基线为统一口径复现）
```bash
python eval_metrics.py ...          # 统一指标重算（RF/SGBANDTI 已校验）
python eval_with_ci.py ...          # bootstrap CI（✅ 已跑，BioSNAP 5 种子）
python aggregate_seeds.py --data biosnap --split random --hop 2   # 5 种子聚合
```


## 五、基线代码（baselines/）

6 个对比基线（MolTrans / MGNDTI / INGNN / TransformerCPI / GNN-CPI / RF）位于包内 `../baselines/`，已适配同一数据、种子与统一指标口径；复现与协议说明见 `../baselines/README.md`。
